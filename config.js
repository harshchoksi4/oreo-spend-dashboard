/** Oreo Spend Dashboard config
 * Paste your Apps Script web app URL after deploy.
 * Leave OREO_TOKEN empty — the app prompts once and stores it in sessionStorage.
 * Pages needs this file for APPS_SCRIPT_URL; do not put a real write token here if the repo is public.
 */
window.OREO_CONFIG = {
  APPS_SCRIPT_URL: "",
  // Optional fallback only; preferred: sessionStorage key "oreo_token"
  OREO_TOKEN: ""
};
