/** Oreo Spend Dashboard config (example)
 * Copy values into config.js after Apps Script deploy.
 * Leave OREO_TOKEN empty — the app prompts once and stores it in sessionStorage.
 * Do not commit a real token if the repo is public; Pages needs config.js for the URL only.
 */
window.OREO_CONFIG = {
  APPS_SCRIPT_URL: "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec",
  OREO_TOKEN: ""
};
